<div class="card__stages">
  <% card.board.columns.each do |column| %>
      <%= tag.span column.name, class: ["card__column-name btn overflow-ellipsis", { "card__column-name--current": column == card.projected_column }] %>
    <% end %>
  </div>
